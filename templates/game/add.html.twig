{% extends 'base.html.twig' %}

{% block title %}{{ 'add_game'|trans }}{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header">
            <h1>{{ 'add_game'|trans }}</h1>
        </div>
        <div class="card-body">
            {{ form_start(form) }}
                {{ form_widget(form) }}
                <button class="btn btn-primary">{{ 'add_game'|trans }}</button>
            {{ form_end(form) }}
            <a href="{{ path('game_list') }}" class="btn btn-secondary mt-3">{{ 'back_to_home'|trans }}</a>
        </div>
    </div>
{% endblock %}